# Agent instructions

Canonical agent instructions for this repo. `CLAUDE.md` is a symlink to this file so Cursor and Claude Code stay in sync — edit here only.

## Agent skills

### Issue tracker

GitHub Issues via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: root `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.

{
  "mcpServers": {
    "pixellab": {
      "command": "npx",
      "args": [
        "mcp-remote@latest",
        "https://api.pixellab.ai/mcp",
        "--transport",
        "http-only",
        "--header",
        "Authorization:Bearer ${PIXELLAB_API_KEY}"
      ]
    }
  }
}

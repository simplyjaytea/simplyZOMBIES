{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://simplyzombies.dev/schemas/zombie.schema.json",
  "title": "Zombie type",
  "description": "A zombie type, per docs/14-zombies.md. Adding one is a JSON entry with zero code, provided every behavior tag it references already exists.",
  "type": "object",
  "required": ["id"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^zombie\\.[a-z0-9_]+$",
      "description": "Stable namespaced id. Permanent once shipped."
    },
    "extends": {
      "type": "string",
      "description": "Id of another zombie type to inherit from."
    },
    "name": { "type": "string" },
    "appearance": { "$ref": "#/$defs/appearance" },
    "introducedInWave": {
      "type": "integer",
      "minimum": 0,
      "description": "Mutation wave that introduces this type. 0 is present from the start."
    },
    "weight": {
      "type": "integer",
      "minimum": 1,
      "description": "Relative share of the mix, drawn by SimRoster.pick_type against the summed weights of the types whose wave has arrived. Omitted counts as 1, the same rule a raider archetype follows. Shipped at 80 / 12 / 8, which is the mix that used to be three constants in roster.gd. The minimum is 1 because a shipped type that can never appear is a dead entry, not a balance knob; pick_type still skips a 0 so a gate fixture can take a kind off the table."
    },
    "variance": {
      "type": "object",
      "description": "How much one body of this kind differs from the next. Drawn per body at spawn on SimRoster's own `zombieLook` stream -- never on the stream that placed it, so adding or retuning any of this leaves every existing campaign's placement and director draws byte-identical (check_m2_variance.gd STREAM). Omit the block entirely and the kind spawns uniform, which is what every kind did before this existed.",
      "additionalProperties": false,
      "properties": {
        "tints": {
          "type": "array",
          "description": "The palette one body's colour is picked from. A modulate over the type's sprite, exactly like `appearance.tint` -- so these are pallor and stain, near-white, not costume colours: a dark entry here darkens the whole body. Empty or absent picks nothing and every body of the kind draws its appearance block's colour.",
          "items": { "type": "string", "pattern": "^#[0-9a-f]{6}$" }
        },
        "body": {
          "type": "number",
          "minimum": 0,
          "maximum": 0.3,
          "description": "Half-width of the per-body size roll: a scale drawn in [1 - body, 1 + body] and applied to every part of both `body` and `bodyMax`, so SimHealth.part_state_of still reads a body against its own maximum and a big one is not born Hurt. 0 spawns every body at the authored size. Capped at 0.3 because past that the kind stops being one kind."
        },
        "crawlers": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Probability that a body is born with its legs already destroyed -- a crawler, reaching the same `crawlFactor` path a shambler whose legs were shot out reaches (SimShambler._speed_of). 0 spawns none."
        }
      }
    },
    "sensory": {
      "type": "object",
      "description": "Channel weights. docs/14-zombies.md#sensory-profiles: there is no single silence.",
      "additionalProperties": false,
      "properties": {
        "noise": { "type": "number", "minimum": 0, "maximum": 1 },
        "light": { "type": "number", "minimum": 0, "maximum": 1 },
        "scent": { "type": "number", "minimum": 0, "maximum": 1 }
      }
    },
    "locomotion": {
      "type": "object",
      "description": "How fast this type moves, in each of the states it has. `speed` is a multiplier on a human walk; the rest are fractions of this type's own `speed`, so a faster type is faster in every state without four numbers to keep in agreement. docs/29-movement-and-stances.md: a zombie's speeds are fields in its JSON entry rather than constants in a module.",
      "additionalProperties": false,
      "properties": {
        "speed": {
          "type": "number",
          "minimum": 0,
          "description": "Purposeful movement -- closing on a stimulus. A multiplier on a human walk, so 0.8 is slower than you walk whatever the pace is set to, which is what makes retreat a real option."
        },
        "wander": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Aimless drift, as a fraction of `speed`. Idle bodies barely move."
        },
        "mill": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Investigating on arrival, as a fraction of `speed`. Slower than drifting."
        },
        "crawl": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "What is left once locomotion is destroyed, as a fraction of `speed`. docs/14-zombies.md#damage-model: a zombie with a destroyed pelvis crawls, is quiet, and is still perfectly capable of biting an ankle."
        }
      }
    },
    "body": {
      "type": "object",
      "description": "Health by body part. Meaningful damage is to the head or to locomotion.",
      "additionalProperties": { "type": "number", "minimum": 0 }
    },
    "worn": {
      "type": "array",
      "description": "Item ids this kind's bodies wear. SimRoster.spawn_zombie gives such a body an inventory, equips each id into the slot its own base declares, and marks it `lootKit` so recruits._drop_kit leaves the gear on the floor when the body goes down -- the SimRecruits._turn_with_kit precedent, which is how a zombie has carried things since a colonist could turn. What it buys is armour on something that is not a survivor: SimInfection.armor_coverage_of reads `equipment` and never asked whose it was, and SimHealth.armor_damage_factor resists with it inside `damage_part`, so an armoured kind costs no new arithmetic at all (docs/14's second wave: accumulated debris and gear, resists light weapons). A zombie body is head/torso/legs, so an `armor` block naming a survivor's sided limbs covers nothing here and is neither an error nor any use. An id no item registry knows fails check_m2_armored.gd's WORN lane rather than spawning a body wearing nothing.",
      "items": {
        "type": "string",
        "pattern": "^item\\.[a-z0-9_]+(\\.[a-z0-9_]+)*$"
      }
    },
    "grab": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "strength": { "type": "number", "minimum": 0 }
      }
    },
    "spread": {
      "type": "object",
      "description": "Persistent per-individual angular bias on gradient ascent, in radians. Without it they form conga lines rather than a horde -- docs/14-zombies.md#gradient-ascent-is-not-sufficient-on-its-own.",
      "additionalProperties": false,
      "properties": {
        "radians": { "type": "number", "minimum": 0 }
      }
    },
    "emits": {
      "type": "array",
      "items": { "$ref": "#/$defs/emission" }
    },
    "behaviors": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Behavior tags. Every one must be implemented, or load fails."
    },
    "alarm": {
      "type": "object",
      "description": "Screamer-style relay. docs/14: converts a small mistake into a regional event.",
      "additionalProperties": false,
      "properties": {
        "magnitude": { "type": "number", "minimum": 0 },
        "relay": { "type": "boolean" },
        "cooldownTicks": { "type": "integer", "minimum": 0 }
      }
    }
  },
  "$defs": {
    "appearance": {
      "type": "object",
      "description": "What this looks like. docs/20: adding a type is a JSON entry with zero code, and appearance was the last axis where that was untrue -- presentation used to pick colours by type id in the draw loop. Omit it entirely and the renderer falls back to its role colours, which is the normal state until art exists.",
      "additionalProperties": false,
      "properties": {
        "sprite": {
          "type": "string",
          "pattern": "^[a-z0-9_.]+$",
          "description": "Registry key, resolved to assets/sprites/<key>.png by presentation/appearance.gd. A key, not a path, so content stays engine-agnostic. A key naming a file that does not exist fails the appearance gate rather than silently drawing nothing."
        },
        "tint": {
          "type": "string",
          "pattern": "^#[0-9a-f]{6}$",
          "description": "Colour for the procedural fallback, and a modulate over the sprite when one exists."
        }
      }
    },
    "emission": {
      "type": "object",
      "required": ["channel", "magnitude"],
      "additionalProperties": false,
      "properties": {
        "channel": { "enum": ["noise", "light", "scent"] },
        "magnitude": {
          "type": "number",
          "minimum": 0,
          "description": "Reach in metres x 0.7 -- docs/03-attention.md#scale-and-calibration."
        }
      }
    }
  }
}

Use case: stylized-concept. Production sprite sheet for an original bleak top-down pixel survival game inspired by ZERO Sievert. Premium deliberately hand-placed chunky pixel clusters, very low resolution look: each logical pixel is a crisp 8x8 square on this 1024x1024 image. Restrained 3-4 shades per material. No antialiasing, no blur, no soft glow, no gradients, no text, no labels, no borders, no weapons or characters. TRUE TRANSPARENT RGBA background, completely empty alpha between sprites, not a checkerboard drawing. Exact 4 columns x 4 rows, equal 256x256 cells, each isolated with generous margins. Each row is one four-frame animation, time left to right. Different actual shapes each frame, never duplicate translated sprites. Every sprite remains strictly within its own cell. Each row uses fixed local contact point(128,144), never shift or recenter the sprite's phases. Row1 concrete impact: frame1 dense small warm gray puff and2chips, frame2 medium angular slate-gray dust burst expanding to130px wide, frame3 wider broken cluster of separated gray blocks spreading160px with fewer central pixels, frame4 only sparse tiny fading light-gray dust motes. Row2 wood impact: frame1 small pale ochre splinter burst, frame2 6 brown/tan wooden pointed chips radiating from contact, frame3 chips spread across170px with split cloud, frame4 few tiny dark brown falling chips. Row3 stylized non-graphic blood hit, NO bodies: frame1 compact rusty dark red fleck, frame2 asymmetric red pixel splash with6droplets, frame3 8 separated smaller burgundy droplets spreading150px, frame4 few tiny falling dark red flecks. No black outlines on light dust; darker rim clusters on chunks. Row4 water impact splash: frame1 small pale-blue crown, frame2 angular blue-white splash with central column and side droplets, frame3 wider separated cool-blue droplets with tiny flattened ripple at impact, frame4 low broken pale-blue elliptical ripple and two motes. Chunky pixel clusters, readable silhouettes. True alpha transparency.

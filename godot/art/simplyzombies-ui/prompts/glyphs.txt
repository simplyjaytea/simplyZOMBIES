Use case: stylized-concept. Production game UI GLYPH atlas, NOT a screenshot or mockup.
Create 32 original pixel glyphs for the approved simplyZOMBIES interface in the references. One RGBA PNG, TRUE TRANSPARENT background, exactly 8 columns by 4 rows on a 1536x768 canvas, equal square 192x192 cells, one glyph centered in each cell with generous transparent gutters. No text, labels, captions, frames, tile backgrounds, grid lines or checkerboard. True transparent alpha around each separate glyph.
Each glyph is designed on a 16x16 native pixel grid, displayed here at a large integer enlargement. Crisp square pixel clusters, strong silhouette, restrained cream #c8c2a6 highlights with olive gray #807a63 shadow and near-black edging. Two or three flat colors at most, simple readable silhouettes matching the approved pixel UI. No smooth curves, no gradients, no tiny photographic details. Most glyphs fit within a 12x12 area on their 16x16 logical canvas. Keep their visual weight consistent. These are small empty-slot/action symbols, not full colored inventory objects.
Exact order left-to-right top-to-bottom:
ROW 1: simple steel helmet; eyeglasses; respirator mask; glove; belt buckle; upright knife (primary weapon); utility vest; short-sleeve shirt.
ROW 2: trousers; pair of boots; backpack; small holstered sidearm (secondary weapon); open inventory bag; simple anatomical person silhouette (condition); crossed hammer and spanner (work); three connected square nodes (skills).
ROW 3: settings cog with six blocky teeth; open notebook (journal); two pause bars; close X; up chevron; down chevron; left chevron; right chevron.
ROW 4: magnifying glass (inspect); open palm (use); four-direction arrow (move); downward arrow above a ground line (drop); bent rotation arrow (rotate); magnifying glass over a small box (search); closed padlock; warning triangle with a short exclamation mark.
Every glyph distinct and clean, exactly 32, each centered in its own cell. Original artwork, no copied game UI.

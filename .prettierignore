node_modules/
dist/
package-lock.json
godot/
godot/parity/expected/
.scratch/

# The docs are hand-wrapped prose; prettier's reflow fights the authored line breaks.
docs/
*.md

# The design records under .hermes/plans/ are artefacts rather than source: the HUD mockups
# are canvas-editor artboards whose absolutely-positioned divs a reflow would rewrite without
# improving, and the canvas.json beside them is the editor's own file. Same reasoning as docs/.
.hermes/
